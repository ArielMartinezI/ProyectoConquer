{
  "name": "project-htmlycss",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist",
    "predeploy": "npm run build && cp *.html dist/ && cp -r sass/stylesheets/cursos/*.html dist/ && cp -r sass/stylesheets/noticias/*.html dist/"

  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sass": "^1.90.0",
    "vite": "^7.1.2"
  }
}
